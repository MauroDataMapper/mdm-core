import javax.servlet.http.HttpSession

model {
    Collection<HttpSession> httpSessionCollection
}
json {
    count httpSessionCollection?.size() ?: 0
    items g.render(httpSessionCollection ?: [])
}
